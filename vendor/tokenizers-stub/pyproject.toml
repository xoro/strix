[project]
name = "tokenizers"
version = "0.22.2+strix"
description = "Import shim; Strix sets litellm.disable_hf_tokenizer_download on FreeBSD"
requires-python = ">=3.12"
license = { text = "Apache-2.0" }
authors = [{ name = "Strix", email = "hi@usestrix.com" }]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["tokenizers"]
